{% extends "layout.html.twig" %}

{% block content %}
    <table>
        <tr>
            <td>Nom de l'utilisateur</td>
            <td>Email</td>
            <td>Lieu de résidence</td>
            <td>Description</td>
        </tr>
        {% for user in users %}
            <tr>
                <td>{{user.username}}</td>
                <td>{{user.email}}</td>
                <td><button><a href="mailto:{{ user.email }}">Contacter</a></button></td>
            </tr>
        {% endfor %}
    </table>
{% endblock %}
